# v2 — Surprise cut (current)

The latest surprise cut. Switched away from v1.5's alternate stills to the **real
static orchestration diagram** + group-chat overlays. This is the version the
founder flagged as a regression from v1 (dropped most cinematic motion shots, the
telemetry composite, and the founder/code-on-glass reveal; used a static, not
animated, diagram).

- **Script:** `review_room_surprise.py` (copy; canonical at
  `scripts/review_room_surprise.py`).
- **Render:** `../output/v2.mp4` (+ `../output/v2.srt`); frame grabs in `surprise-v1_critique/`.
- **Work dir:** `work_surprise/` (ElevenLabs narration `narr_*`, captions, beats;
  internal `v2_*`/`v3_*` check frames from its own iterations).

## shared material (referenced directly, no local copies)
- from `../raw`: `sample3`, `sample5`, `sample4`, `free-mode-demo.mp4`,
  `anim_group_chat.mp4`.
- borrowed from `../v1/owned`: `shot_04.mp4`, `shot_06.mp4`, `shot_08.mp4` (motion),
  `shot_05.png` (room bg).

## external/
- `diagram_static-paste.png` → the orchestration diagram PNG (beat 7, static).
