# Narration v2.7.2 — paste-ready script (Codex deliverable, ACCEPTED)

Source: Codex, group chat 2026-06-02. Revises [v2.7.1-hybrid]. This is the
APPROVED narration baseline for the v2.7 render. Keeps the debate voiced (A/B),
uses the locked light prove-then-explain turn, ~38–40s voice over 52s.

## Tuple contract (NEW — assembler must be updated to match)
- `VO_LINES`: `(anchor_s, voice_tag, vo_text, caption_text)`
- `DIALOGUE`: `(anchor_s, dur_s, voice_tag, vo_text, caption_text)`
- Voice tags: `N` narrator (Jessica) / `A` debater-promise / `B` debater-pain
- When `caption_text` is shorter than `vo_text`, the caption shows the short form.

```python
# v2.7.2 narration. Captions are explicit; when shorter than VO, use caption_text.
VO_LINES = [
    # 01 bustling hub, 0-5s
    (1.0, "N",
     "An ordinary afternoon at Kneaded A.I. A small studio making local marketing work.",
     "An ordinary afternoon at Kneaded.ai."),

    # 02 debate setup, 5-9s
    (5.3, "N",
     "Today, the team is stuck on one thing: the opening line.",
     "The team is stuck on the opening line."),

    # 04 deck/pitch, 16-21s
    (16.3, "N",
     "Someone turns the argument into a pitch: one clean slide, one recommendation.",
     "Someone turns the argument into a pitch."),

    # 05 founder review, 21-25s
    (21.3, "N",
     "They take it to the founder. He has notes. A lot of notes.",
     "They take it to the founder. He has notes. A lot of notes."),

    # 06 ghost-table turn, 25-30s
    (27.0, "N",
     "But the room was never the company.",
     "But the room was never the company."),

    # 07 diagram begins, 30-36s
    (30.3, "N",
     "There was no team.",
     "There was no team."),

    # 07 diagram resolves, 30-36s
    (33.0, "N",
     "Every role was an agent. One coordinator kept them in sync.",
     "Every role was an agent. One coordinator kept them in sync."),

    # 08 debate replay / group chat, 36-40s
    (36.3, "N",
     "The debate you watched was a group chat between agents.",
     "The debate was a group chat."),

    # 09 deck-agent replay, 40-44s
    (40.3, "N",
     "The presenter was the deck agent, rebuilding the slide from founder notes.",
     "The presenter was the deck agent."),

    # 10 founder isolation, 44-48s
    (44.3, "N",
     "Everyone here is an agent. Except the founder.",
     "Everyone here is an agent. Except the founder."),

    # 11 final slate, 48-52s
    (48.3, "N",
     "One human. A whole agency. This is how Kneaded A.I. works.",
     "This is how Kneaded.ai works."),
]

# v2.7.2 voiced debate. These are spoken by A/B, not captions-only.
DIALOGUE = [
    # 03a close-up A, 9-12.5s — promise/upside
    (9.4, 3.2, "A",
     "Lead with the promise. Save five hours a week on local marketing.",
     "Lead with the promise: save five hours a week on local marketing."),

    # 03b close-up B, 12.5-16s — pain/counter
    (12.9, 3.4, "B",
     "No. Open on the pain. You're drowning in posts you don't have time to write.",
     "Open on the pain: you're drowning in posts you don't have time to write."),
]
```

## Integration notes (Codex + Director)
- `DIALOGUE` must be PROMOTED from caption-only (v2.6) to real audio: generate A/B
  as separately-placed audio clips with voice-specific IDs, merge into the audio
  bed alongside the narrator. Assembler audio loop + SRT builder need the new
  4/5-tuple shape.
- A/B voice IDs to confirm (proposed Adam + Rachel, must be distinct from narrator
  Jessica `cgSgspJ2msm6clMCkdW9`).
